---
layout: layouts/base.vto
bodyClass: body-home
title: Home
image: /og/index.png
lang:
  - ja
  - en
---

<header class="page-header page-header--home">
  <h1 class="sr-only">{{ metas.site }}</h1>
  <div class="home-hero home-hero--profile">
    {{ if it.site_icon }}
    <img
      src="{{ it.site_icon }}"
      alt=""
      class="home-avatar"
      width="80"
      height="80"
      decoding="async"
    />
    {{ /if }}
    <nav class="home-profile" aria-label="{{ i18n.home.profile_aria }}">
      <div class="home-profile-row">
        {{ if home.tagline }}
        <p class="home-profile-tagline">{{ home.tagline }}</p>
        {{ /if }}
        <ul class="home-profile-links">
          {{ for link of home.profile_links }}
          <li>
            <a
              class="home-profile-icon-link"
              href="{{ link.href }}"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="{{ link.label }}"
            >
              {{ include "partials/home-sns-icon.vto" { icon: link.icon } }}
            </a>
          </li>
          {{ /for }}
        </ul>
      </div>
      {{ if home.profile_intro_links?.length }}
      <p class="home-profile-intro">
        {{ for link of home.profile_intro_links }}
        <a class="home-profile-intro-link" href="{{ link.href }}">{{ link.label }}</a>
        {{ /for }}
      </p>
      {{ /if }}
    </nav>
  </div>
</header>

<section class="post-grid" aria-label="{{ i18n.home.posts_aria }}">
  {{ for post of search.pages(`type=post lang=${it.lang}`, "lastUpdated=desc") }}
  <article class="post-card">
    <a href="{{ post.url }}" class="post-card-link">
      <img
        class="post-card-thumb"
        src="{{ post.basename |> thumbUrl }}"
        alt=""
        width="960"
        height="540"
        loading="lazy"
        decoding="async"
      />
      <span class="sr-only">{{ post.title }}</span>
    </a>
  </article>
  {{ /for }}
</section>

{{ if it.lang == "ja" }}
<p class="home-archive-link"><a href="/archive/">{{ i18n.nav.archive_link }}</a></p>
{{ /if }}
