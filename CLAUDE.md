# Claude Code 向けの運用指示

リポジトリの運用ルール（ディレクトリ構成・フロントマター・コマンド・文体の参照先）は
**`AGENTS.md` を単一ソース**とする。ここでは重複させず読み込む。

@AGENTS.md

---

以下は Claude のモデル挙動に対するチューニング。リポジトリの事実ではなく、
**振る舞いの調整**なのでこのファイルに置く（`AGENTS.md` には書かない）。

## 応答の長さ

会話上の応答は焦点を絞って簡潔にする。前置き・注意書き・免責は短くまとめ、
本題に紙面を使う。説明を求められたときは、詳細な解説を明示的に頼まれない限り
高レベルの要点を返す。

<tone_preference>
Keep outputs reasonably concise.
</tone_preference>

## 成果物（ファイル）の長さ

記事やドキュメントの長さは**中身に見合わせる**。短くすることが目的ではない
（記事の文体・構成は `prompts/yasuna-style-prompt.md` に従う）。
ただし埋め草セクション・同内容の要約の繰り返し・定型の締めで量を稼がない。

## サブエージェントへの委譲

- 委譲するのは、**独立して並列化できる大きな作業**だけ（例: 複数記事にまたがる横断調査、
  過去記事全体の表記ゆれ調査）。
- 数回のツール呼び出しで終わる作業は委譲しない。**記事1本の執筆、1ファイルの修正、
  フロントマターの追加は自分でやる。**
- **自分の作業の検証・再確認のためにサブエージェントを使わない。**
- 1体で足りるなら1体。起動数は少なく保つ。

## 検証について

作業の確認は必要な範囲で自分で行う。このリポジトリの指示に
「最後に検証ステップを入れる」「検証用のエージェントを立てて確認させる」
「念のため再確認する」といった指示は**意図的に置いていない**。追加しないこと。
過剰な検証はトークンを消費するだけで品質は上がらない。

## 記事のレビューを頼まれたとき

指摘を先に絞らない。**気づいたものは全部挙げてから、重要度で選別する**という順序で行う
（「重大なものだけ挙げて」という絞り込みを先に適用すると、拾えるはずの指摘が落ちる）。
