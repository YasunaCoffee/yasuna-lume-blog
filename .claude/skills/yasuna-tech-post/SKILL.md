---
name: yasuna-tech-post
description: >-
  yasuna のテックブログ（Lume）に yasuna 文体で新しい記事を書く。「ブログ記事を書いて」
  「yasuna-tech に記事を追加」「下書きを作って」「論文読んでみた記事を書いて」
  「Zenn風の記事のたたきを作って」などのとき、または yasuna-tech リポジトリの
  drafts/ や src/posts/ に Markdown 記事を追加・公開したいときに使う。
  実行時に prompts/yasuna-style-prompt.md を読んで文体・トーンを反映し、
  フロントマター付きの記事を作成する。
---

# yasuna-tech 記事執筆スキル

yasuna のテックブログ（Deno + Lume）に、**yasuna 文体**で記事を書くためのスキル。

## リポジトリの場所

- ローカル: `/home/yasuna/dev/yasuna-tech`
- 無ければ取得: `git clone https://github.com/YasunaCoffee/yasuna-tech.git`（`/home/yasuna/dev/` 配下を推奨）

以降の手順はこのリポジトリのルートで作業する。**まず `AGENTS.md` を読んで運用全体を把握する**こと。

## 手順

### 1. テーマを確認する

ユーザーから **テーマ・狙い・公開予定日** を確認する。ユーザーの実体験と矛盾する事実は捏造しない。不明点は質問する。

### 2. 文体を読み込む

**必ず実行時に `prompts/yasuna-style-prompt.md` を読む**（このスキルに文体を複製しない＝単一ソースを保つ）。要点:

- 基本は **です・ます**。感情には「！」や軽い口語を少量。自己開示は率直に、上から目線は避ける。
- 冒頭は状況を短く宣言（`端的に言うと` / `結論、`）。`# はじめに` / `# まとめ` を使う。
- 強調は **太字** を少しだけ。`ちなみに`（脱線）/ `そこで`・`また`（話題転換）。
- 外部リンクは URL を行でそのまま置くことも多い。
- タイトルに Noto Sans JP で出ない文字（`――` 等）を使わない。区切りは `：`。
- **`category: "論文読んでみた"` の記事**は、同ファイルの専用ルールに従う（論文パートと自分パートを `---` で分離、論文に書いていないことを書かない、AITuber 文脈に紐付け、VTuber と混同しない）。

### 3. フロントマターを付ける

`templates/post.md` を雛形にする。ファイル名・スラッグは **kebab-case**（`foo-bar.md` → `/posts/foo-bar/`）。

```yaml
---
title: "記事タイトル"
date: "YYYY-MM-DD"
author: yasuna
emoji: "📝"
category: "カテゴリ名"   # 省略時は先頭の tags がサムネ上のカテゴリ表示になる
tags:
  - タグ1
  - タグ2
draft: false
---
```

- `emoji` … 見出し横の絵文字。サムネ・OGP（`scripts/generate-og.ts`）にも使う。
- このブログは **Zenn ではない**。`:::message` はそのままでは効かないことがある。必要なら通常の引用・見出しで代用する。

### 4. 保存先を選ぶ

- **まだ公開しない** → `drafts/{slug}.md`（ビルド対象外）。
- **公開する** → `src/posts/{slug}.md`。ユーザー指示がない限り、公開用は `src/posts/` のみ触る。

### 5. 実績に載せる場合（任意）

その記事を **2026 年の実績**に載せたいときは、同じ作業のなかで `src/posts/jisseki-2026.md` を更新する。手順は `.cursor/skills/jisseki-2026-update/SKILL.md` に従う（新しい番号セクション追加、冒頭の件数更新、一覧表に行追加）。「実績に載せない」と言われた記事は対象外。

### 6. 確認

プレビュー・ビルドが必要なら **`yasuna-tech-build` スキル**を使う（`deno task dev` でサムネ生成込みプレビュー）。

## 記事の長さ

**内容に見合った長さにする。** 短くすることが目的ではなく、体験や所感を削る必要はない。
ただし埋め草セクション・同内容の要約の繰り返し・「まとめ」の水増しで量を稼がない。
書くことが尽きたらそこで終わる。

## やらないこと

- `_site/` を手で編集しない（ビルド成果物）。
- 文体プロンプトにない別ペルソナに勝手に切り替えない。
- 根拠のない大言壮語・「いかがでしたか」の連発・誹謗中傷。
